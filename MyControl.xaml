<UserControl x:Class="AugustaStateUniversity.SeniorCapstoneIgnoreList.MyControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vsfx="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.10.0"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             Name="MyToolWindow"
             Background="{DynamicResource {x:Static vsfx:VsBrushes.ToolWindowBackgroundKey}}">
    <StackPanel Height="282" Name="stackPanel1" Width="281">
        <StackPanel Height="54" Name="stackPanel2" Width="264" Orientation="Horizontal" Margin="0">
            <TextBox Height="23" Name="ignoreListTextBox" Width="120" MinWidth="150" Margin="10" />
            <Button Content="Add" Height="23" Name="ignoreListAddButton" Width="75" Margin="10" IsManipulationEnabled="False" Click="ignoreListAddButton_Click" IsDefault="True" />
        </StackPanel>
        <ListView Height="209" Name="ignoreList" Width="262" AllowDrop="False" />
    </StackPanel>
</UserControl>
